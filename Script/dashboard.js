// Main dashboard initialization and shared functions
function initializeDashboard() {
    // Initialize tabs
    const triggerTabList = document.querySelectorAll('[data-bs-toggle="pill"]');
    // ... rest of initialization code
}